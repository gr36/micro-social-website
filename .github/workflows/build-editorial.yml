name: Build Micro Wrapped feed

on:
  push:
    branches: [main]
    paths:
      - "editorial/issues/**"
      - "editorial/build.py"
      - "editorial/validate.py"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install pyyaml
      - run: python3 editorial/build.py
      - run: python3 editorial/validate.py editorial/feed.json
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add editorial/feed.json
          git diff --cached --quiet || git commit -m "Build Micro Wrapped feed"
          git push
