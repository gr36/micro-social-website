# Micro Wrapped

`feed.json` is what the app reads. `issues/` holds the editions, one Markdown file each. Both
are written by the [desk](https://github.com/gr36/micro-wrapped-desk); nothing here is edited
by hand. Artwork is in `static/images/issues/`.
